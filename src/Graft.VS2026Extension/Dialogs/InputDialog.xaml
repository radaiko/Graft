<Window x:Class="Graft.VS2026Extension.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Label -->
        <TextBlock Grid.Row="0" Text="{Binding LabelText}" Margin="0,0,0,4" />

        <!-- Text input or ComboBox -->
        <TextBox Grid.Row="1" x:Name="InputTextBox"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 Visibility="{Binding ShowTextBox}"
                 Margin="0,0,0,8" Padding="4" />
        <ComboBox Grid.Row="1" x:Name="InputComboBox"
                  IsEditable="True"
                  Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                  ItemsSource="{Binding ComboBoxItems}"
                  Visibility="{Binding ShowComboBox}"
                  Margin="0,0,0,8" Padding="4" />

        <!-- Optional checkbox -->
        <CheckBox Grid.Row="2"
                  Content="{Binding CheckBoxText}"
                  IsChecked="{Binding IsChecked}"
                  Visibility="{Binding CheckBoxVisibility}"
                  Margin="0,0,0,8" />

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0"
                    IsDefault="True" Click="OnOkClick" />
            <Button Content="Cancel" Width="80"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
